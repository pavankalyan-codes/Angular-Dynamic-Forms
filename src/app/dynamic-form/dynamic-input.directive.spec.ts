import { DynamicInputDirective } from './dynamic-input.directive';

describe('DynamicInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicInputDirective();
    expect(directive).toBeTruthy();
  });
});
