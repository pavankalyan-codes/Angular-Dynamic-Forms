import { InputTemplateBase } from './input-template-base';

describe('InputTemplateBase', () => {
  it('should create an instance', () => {
    expect(new InputTemplateBase()).toBeTruthy();
  });
});
